Создать класс Interval для работы с интервалами типа (a, b), [a, b], (a, b], [a, b)
	методы
		bool intersect(Interval); - пересекаются ли два интервала;
		bool intersect(int x) - входит ли точка в интервал;